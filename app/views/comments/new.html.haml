%h2 Enter a new comment for #{@user.name} 
= form_tag user_comments_path, :method => :post do
	=render :partial => 'form'  
	= hidden_field :comment, :user_id, value: @user.id   
	= submit_tag 'Save Changes'