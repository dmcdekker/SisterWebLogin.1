%h2 #{@user.name}, enter a new blog
= form_tag user_userblogs_path, :method => :post do
	=render :partial => 'form'  
	= hidden_field :userblog, :user_id, value: @user.id   
	= submit_tag 'Save Changes'
